% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouping.R
\name{group_markers}
\alias{group_markers}
\title{Group markers into linkage groups}
\usage{
group_markers(onemap_obj, lod = 3, max_rf = 0.5, map_fun = "kosambi")
}
\arguments{
\item{onemap_obj}{An \code{onemap} object returned by \code{\link[=validate_lm_file]{validate_lm_file()}} or
\code{\link[onemap:read_mapmaker]{onemap::read_mapmaker()}}.}

\item{lod}{Numeric. LOD score threshold for declaring linkage. Passed to
\code{\link[onemap:rf_2pts]{onemap::rf_2pts()}}. Default \code{3}.}

\item{max_rf}{Numeric in [0, 0.5]. Maximum recombination frequency for
declaring linkage. Passed to \code{\link[onemap:rf_2pts]{onemap::rf_2pts()}}. Default \code{0.5}.}

\item{map_fun}{Character. Mapping function. One of \code{"kosambi"} (default)
or \code{"haldane"}. Passed to \code{\link[onemap:set_map_fun]{onemap::set_map_fun()}}.}
}
\value{
An object of class \code{"sequence"} (as returned by \code{\link[onemap:group]{onemap::group()}})
with field \verb{$n.groups} giving the number of linkage groups detected.
}
\description{
Performs two-point linkage analysis with \code{\link[onemap:rf_2pts]{onemap::rf_2pts()}} and groups
markers into linkage groups with \code{\link[onemap:group]{onemap::group()}}.  This is Module 2 of
the Linkmapper analytical workflow.
}
\details{
To obtain a data-suggested LOD threshold instead of supplying a fixed value,
call \code{\link[onemap:suggest_lod]{onemap::suggest_lod()}} on the \code{onemap} object first and pass the
result as \code{lod}.
}
\examples{
\dontrun{
f2data <- validate_lm_file("path/to/mydata.txt")

# Use a fixed LOD threshold
lgs <- group_markers(f2data, lod = 3, max_rf = 0.5)
lgs$n.groups

# Or let onemap suggest the LOD
suggested <- onemap::suggest_lod(f2data)
lgs <- group_markers(f2data, lod = suggested, max_rf = 0.5)
}

}
